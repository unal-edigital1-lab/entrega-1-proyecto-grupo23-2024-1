`timescale 1ns / 1ps
module tiempo (

  input Acelerar,
  input clk,
  output led
  
  
  
  );

  assign enable = cfreq[32]
  
  
  always @(posedge clk)begin
  
  
  
  end
  
  
  always @(posedge enable)begin
  
  
  
  end
  
  
  

endmodule